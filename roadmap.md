# roadmap — 3D 象棋對局場(xiangqi-arena)(**2026-09-07 對齊**)

## ✅ 已完成(別重做)

- **💡 提示不建議「白做工的交換」(2026-09-07,HFP 機・session 0906-3D西洋棋棋字名)**:
  由來——使用者在 3D 幻影西洋棋退件「提示叫我吃、吃完又被別的子吃回,等於交換被吃」⇒ 做了一輪**全棋類提示體檢**。
  本站的兩條經典病因 0904 重寫時就修掉了(PST + quiescence),所以這輪加的是**第三條、四站統一的規矩**:
  `LEVELS.hint.tradeMargin = 50`(半個卒)—— 吃子要比最好的安靜手多賺半個卒才推薦,否則建議走位;
  AI 對手三檔完全不受這條約束。實作是 `_hintRoot()` 兩段式根層 + `_captureGain()` 純子力交換試算。
  ★★ **順手抓到一個藏很久的真 bug(比門檻本身更有價值)**:「算到殺棋就收工」的條件漏了有限數檢查——
     沒有吃子過門檻時 `noisyScore` 還是 `-Infinity`,而 `Math.abs(-Infinity) > MATE` **恆真**
     ⇒ **提示每一局都只搜到 depth 1 就交卷**(等於只看一步)。修好後穩定搜到 depth 4~6。
  ★ 三個實作上踩到的坑(都寫進 ai.js 註解):
    ・不能沿用 `calculateBestMove` 的 `scored` 去比門檻:迭代加深根層只有「抬高 alpha 的那一手」拿到精確分,
      其餘是**上界**;而 `_orderMoves` 讓吃子先搜先當冠軍 ⇒ 安靜手拿到貼著 alpha 的上界 ⇒ 門檻永遠高於冠軍,
      **任何吃子都過不了關**(症狀:每局都印「0 手值得推薦」,連白吃一支車都被擋)。
      ⇒ 正解:安靜手先搜(rising alpha ⇒ 冠軍精確),再用「安靜手 + 門檻」當吃子的起始 alpha。
    ・子力關要**按需計算**:第一版在搜尋前把每個吃子都算一遍,1400ms 預算被它吃光(log 印 depth 1/7)。
    ・出題兩個坑:兩個王不可排同一直線(會飛將,王吃王才是正解);黑方也不可只剩光桿王
      (車+帥必殺會蓋過吃子,測試會紅在自己出的題目上)。
  測試:新增 `test/hint.mjs` 6/0(20 個隨機中局驗「建議的吃子交換算到底都有賺」+ 白吃不可放過 + 零隨機);
  `npm test` 併入它。

- v1 重建版(0902):網址不變、從此有源碼;五種開局譜 / 盤面快照存讀檔 / 2D-3D 視角 / 長將規則 / 悔棋 /
  💡 提示 / 📅 每日殘局一組 5 題 / 統計打點 / `npm run stage` 乾淨部署包。
- v2 ⛶ 全螢幕棋盤(0902):面板級全螢幕 + 小工具列(提示/後悔/視角/離開)+ iPhone 假全螢幕 + ResizeObserver;
  browser-check 31→37。

- 🧠 AI 引擎重寫 + 📅 題庫全換(0904,agape250 機;使用者原話「AI 太笨了,常給一些沒用的提示」「每日殘局太簡單了,一點難度都沒有」):
  ① **規則層補「不得自將」**:`gameLogic.isValidMove` 原本只問走法規則,不問「走完自己會不會被將」;
     `isInCheck()` 早就寫好卻從沒被呼叫(原註解「為 Milestone 4 及 AI 預留」)⇒ 連玩家都能走出送將的棋。
  ② **ai.js 重寫**:合法走法(過濾自將/飛將)→ 將軍・將死・困斃 → PST 位置評估 → MVV-LVA 排序
     → alpha-beta + 靜態搜尋(quiescence)→ 迭代加深(時間預算,想不完就用上一層結果)。
     搜尋內部走「偽合法 + 吃王即勝」求速度,**真正的合法性只在根節點做一次**,所以交出去的那手永遠合法。
  ③ **提示獨立成 `hint` 檔**(比高級再深一層、關掉同分隨機 ⇒ 同局面同答案)。
  ④ **題庫 18 題全換成機器窮舉驗過的「N 手連將殺」**(2手×2 3手×5 4手×6 5手×5):
     黑方一律有反擊子(舊題庫 16 題**一顆都沒有**)、開局 28~54 步可應;
     `hint` 只講規則不講解法(舊的直接寫「前炮就是後炮的炮架!」)。
  ⑤ test/daily.mjs 第 ③ 節從「AI 實打 3 試 ≥2 勝」改成**窮舉求解**:是保證不是抽樣,而且快(舊驗法配新引擎要跑好幾小時)。
  量到的:同檔位新舊對打 8 盤 **新 6 : 舊 0.5**;測試 129→251。
  ★ 過程中踩到的兩個坑(都寫進 ai.js 註解):
    ・靜態搜尋被將軍時回傳了傳進來的 `alpha`(全窗=`-Infinity`)⇒ 父節點取負變 **+Infinity**
      ⇒ 引擎認定「能將軍就是必勝」,把車炮一路送去將軍。對打 0:4.5 才逼出來。
    ・「留一點隨手給孩子」誤套到**中級**(4% 亂走);消融實測關掉後 3/4→4/4、子力 −4540→+4180。
      隨機只能給初級。

## 🔜 待做(CP 值 = 價值 ÷ 時間)

| 項 | ⏱ | ★ | 說明 |
|---|---|---|---|
| 手機橫向全螢幕棋盤仍偏小 | 1h | ★★ | 844×390 全螢幕後畫布 824×330,3D 斜視角受高度限制。可在全螢幕時自動切 2D 俯視、或把狀態列縮成一行浮在畫布上 |
| ~~全螢幕裡沒有「重新開局 / 難度 / 每日」~~ ✅ **0905 v8 完工**(6ecccd5:fsToolbar 三顆控件全部「代按右側欄原鈕」,邏輯只有一份;順帶右側欄多一條到單機版 3d-xiangqi.pages.dev 的路) | 30m | ★ | 目前要退出全螢幕才能換設定;使用者反映再把 newGame / daily 加進工具列 |
| 統計打點三層確認 | 20m | ★★ | 已接 psPing / -done / -dwell;用 `beacon-coverage-audit` 對賬一次 |

## 🚫 刻意不做

- 黑方的字轉 180° 朝黑方(實體擺法):使用者要全部朝玩家。
- 連 git 自動部署:直傳站 + stage 流程已穩;同 chess5 0902 拍板「低頻改動不養長期金鑰」。
- 沿用舊站存檔鍵 `xiangqi-3d-save-v3`:另一支引擎的格式,讀進來會靜默重建成別的局面(README 坑 #9)。
